version: "3"
services:
  sass:
    image: "sassv1"
    ports:
      - "80:5000"
    networks: 
      - backend

  mongodb:
    image: "mongosass"
    networks:
      - backend
networks:
  backend:
    external: true
